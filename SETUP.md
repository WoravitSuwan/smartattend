# SmartAttend — คู่มือติดตั้งและใช้งาน

ระบบตรวจสอบการเข้าเรียนอัตโนมัติด้วยการจดจำใบหน้า
มหาวิทยาลัยเทคโนโลยีราชมงคลล้านนา เชียงใหม่

ในชุดนี้มี 2 ส่วน:

| โฟลเดอร์ | คืออะไร | ภาษา |
|---|---|---|
| `smartattend_web/` | เว็บแอปพลิเคชัน (ติดตั้งเป็นแอปมือถือได้) | TypeScript + React |
| `smartattend_pi/` | โปรแกรมบน Raspberry Pi (อัลกอริทึม + ฮาร์ดแวร์) | **Python** |

---

# ส่วนที่ 1 — เว็บแอปพลิเคชัน

## 1.1 ติดตั้งและรันใน VS Code

```bash
cd smartattend_web
npm install
```

สร้างไฟล์ `.env` (คัดลอกจาก `.env.example`) แล้วใส่ค่าจริง

จากนั้น **กด F5 ใน VS Code** เลือก `▶ รันเว็บ + เปิดเบราว์เซอร์` — เว็บจะรันพร้อมเปิดเบราว์เซอร์ให้เอง
หรือสั่งเองด้วย `npm run dev`

> แก้โค้ดแล้วกด Save เว็บจะอัปเดตทันทีโดยไม่ต้องรีเฟรช

## 1.2 ทำลิงก์ดาวน์โหลดเป็นแอปมือถือ (PWA)

โปรเจกต์นี้ตั้งค่า PWA ไว้ให้แล้ว **ลิงก์เดียวใช้ได้ทั้ง iPhone และ Android ฟรี ไม่ต้องผ่าน App Store**

### ขั้นตอน

```bash
npm run build      # สร้างไฟล์สำหรับใช้งานจริง
```

จากนั้นนำโฟลเดอร์ `dist/` ขึ้นโฮสต์ที่ใดที่หนึ่ง เลือกได้ตามสะดวก:

| บริการ | คำสั่ง | ค่าใช้จ่าย |
|---|---|---|
| Vercel | `npx vercel --prod` | ฟรี |
| Cloudflare Pages | `npx wrangler pages deploy dist` | ฟรี |
| Netlify | `npx netlify deploy --prod --dir dist` | ฟรี |

จะได้ลิงก์เช่น `https://smartattend.vercel.app` — **นี่คือลิงก์ดาวน์โหลดแอปของคุณ**

> **ก่อน deploy ต้องตั้งค่า Environment Variables บนแพลตฟอร์มที่เลือกด้วย**
> (`VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`, `VITE_SUPABASE_PROJECT_ID` — ดูค่าได้จาก `.env`)
> ไม่งั้นแอปที่ deploy แล้วจะเชื่อม Supabase ไม่ได้
>
> ไฟล์ `vercel.json` และ `public/_redirects` เตรียมไว้ให้แล้วในโปรเจกต์ ทำให้
> เส้นทางในแอป (เช่น `/student/history`) ใช้งานได้ปกติแม้ผู้ใช้รีเฟรชหน้าตรง ๆ
> หรือเปิดลิงก์นั้นมาจากที่อื่น — ไม่ต้องตั้งค่า rewrite เพิ่มเองทั้งบน Vercel และ
> Netlify/Cloudflare Pages

### วิธีที่ผู้ใช้ติดตั้ง

**Android** — เปิดลิงก์ใน Chrome จะมีแถบ "ติดตั้งเป็นแอป" เด้งขึ้นมา กดติดตั้งได้เลย

**iPhone** — ต้องเปิดด้วย **Safari** เท่านั้น แล้วแตะปุ่มแชร์ → เลือก "เพิ่มไปยังหน้าจอโฮม"
(แอปในเครื่องจะแสดงคำแนะนำนี้ให้อัตโนมัติ)

หลังติดตั้งจะได้ไอคอนบนหน้าจอ เปิดแล้วเต็มจอ ไม่มีแถบเบราว์เซอร์ ใช้งานเหมือนแอปทั่วไป

> **ข้อดีสำคัญ:** แก้โค้ดแล้ว deploy ใหม่ ผู้ใช้ได้เวอร์ชันล่าสุดอัตโนมัติ
> ไม่ต้องให้ทุกคนมาโหลดไฟล์ใหม่ — ต่างจาก APK ที่ต้องส่งไฟล์ให้โหลดใหม่ทุกครั้ง

### ถ้าอยากได้ไฟล์ APK จริง ๆ

```bash
npm i -D @capacitor/core @capacitor/cli @capacitor/android
npx cap init SmartAttend th.ac.rmutl.smartattend
npx cap add android
npm run build && npx cap sync
npx cap open android        # เปิด Android Studio แล้วกด Build > Build APK
```

> **iOS แจกไฟล์ตรงไม่ได้** ไม่ว่าวิธีใด ต้องสมัคร Apple Developer Program
> ปีละ 99 ดอลลาร์ แล้วแจกผ่าน TestFlight เท่านั้น — เป็นข้อบังคับของ Apple
> ดังนั้น **PWA คือทางที่คุ้มที่สุดสำหรับ iOS**

## 1.3 ตั้งค่า Supabase CLI (deploy edge function เองได้จากเครื่อง ไม่ต้อง copy-paste)

โค้ดที่อยู่ในโฟลเดอร์ `smartattend_web/supabase/functions/` (เช่น `import-course-roster`,
`start-class-session`) รันอยู่บน Supabase โดยตรง **ไม่ได้ deploy อัตโนมัติตอน push ขึ้น GitHub**
ต้องสั่ง deploy แยกทุกครั้งที่แก้โค้ดฝั่งนี้ ทำครั้งแรกครั้งเดียวตามนี้ แล้วครั้งต่อ ๆ ไปเหลือแค่คำสั่งเดียว

```bash
cd smartattend_web

# 1) ติดตั้ง Supabase CLI (ทำครั้งเดียวต่อเครื่อง)
npm install -g supabase

# 2) ล็อกอินด้วยบัญชี Supabase ของคุณ — จะเปิดเบราว์เซอร์ให้กดยืนยัน
supabase login

# 3) เชื่อมโฟลเดอร์นี้เข้ากับโปรเจกต์ Supabase จริง (project ref ดูได้จาก
#    Supabase Dashboard > Project Settings > General > Reference ID
#    หรือจากค่า VITE_SUPABASE_PROJECT_ID ในไฟล์ .env)
supabase link --project-ref grxjnfdngugtuyhkinaj
```

จากนั้นทุกครั้งที่แก้โค้ดใน `supabase/functions/<ชื่อฟังก์ชัน>/index.ts` ให้ deploy ด้วยคำสั่งเดียว:

```bash
supabase functions deploy <ชื่อฟังก์ชัน>
# เช่น
supabase functions deploy import-course-roster
supabase functions deploy start-class-session
```

> ไม่ต้อง copy โค้ดไปวางใน Dashboard อีกต่อไป — คำสั่งนี้อ่านไฟล์จากเครื่องแล้วอัปโหลดให้เลย
> ถ้าแก้ฐานข้อมูล (ไฟล์ใน `supabase/migrations/`) ก็ใช้ `supabase db push` ในลักษณะเดียวกัน

---

# ส่วนที่ 2 — โปรแกรม Python บน Raspberry Pi

ส่วนนี้คือ **อัลกอริทึมจดจำใบหน้าและโค้ดที่ติดต่อกับฮาร์ดแวร์** เขียนด้วย Python ทั้งหมด

## 2.1 ความแตกต่างของอัลกอริทึมสองเวอร์ชัน

| | เว็บ (TensorFlow.js) | **Pi (Python)** |
|---|---|---|
| วิธีการ | Closed-set Classification | **Embedding-based** |
| โมเดล | CNN ที่เทรนเอง | dlib ResNet (เวกเตอร์ 128 มิติ) |
| เพิ่มนักศึกษาใหม่ | **ต้องเทรนใหม่ทั้งหมด** | **แค่เพิ่มเวกเตอร์ ไม่ต้องเทรน** |
| ปฏิเสธคนแปลกหน้า | ทำไม่ได้ (softmax บังคับผลรวมเป็น 1) | **ทำได้ ด้วยเกณฑ์ระยะห่าง** |
| ประมวลผลที่ | เบราว์เซอร์ของนักศึกษา | Raspberry Pi หน้าห้องเรียน |

การย้ายมาใช้ embedding แก้ข้อจำกัดใหญ่ที่สุดของเวอร์ชันเว็บ คือการที่ต้องเทรนโมเดลใหม่
ทุกครั้งที่มีนักศึกษาเข้าหรือออก

> **หมายเหตุ (แก้ไขแล้ว): ลงทะเบียนผ่านแอปแล้วเช็คชื่อที่ Pi ไม่ผ่าน**
>
> ปัญหาเดิม: นักศึกษาลงทะเบียนใบหน้าผ่านเว็บแอป (อัปโหลดรูป) แล้วมาสแกนที่กล้อง Pi
> ไม่ผ่านเกณฑ์ระยะห่าง (dist) ทั้งที่เป็นคนเดียวกัน — เคยเข้าใจผิดว่าเกิดจาก "กล้องต่างชนิด
> ให้เวกเตอร์ต่างกัน" และลองผ่อนเกณฑ์/ปรับกล้อง/ใช้ CLAHE แล้วไม่สำเร็จ
>
> สาเหตุจริง: `normalizeOriginal()` ใน `smartattend_web/src/lib/dataset-store.ts` ย่อรูป
> ที่อัปโหลดด้วย `ctx.drawImage(img, 0, 0, w, h)` ลงกล่องขนาดคงที่ 240×300 โดยไม่รักษาสัดส่วนภาพ
> ทำให้ใบหน้าถูก "ยืด/บีบ" ผิดสัดส่วนก่อนถูกเก็บเป็น `face_images.image_data` — ซึ่งเป็นรูปที่ Pi
> ดาวน์โหลดไปสร้างเวกเตอร์ dlib โดยตรง ภาพใบหน้าที่บิดเบี้ยวทำให้เวกเตอร์ต่างจากภาพสดที่กล้อง Pi
> จับได้มาก แม้เป็นคนเดียวกันก็ตาม — ไม่เกี่ยวกับชนิดกล้องเลย
>
> การแก้ไข: เพิ่มการตรวจจับใบหน้าฝั่งเบราว์เซอร์ (`@tensorflow-models/blazeface`) ใน
> `src/lib/face-crop.ts` เพื่อครอปภาพเป็นสี่เหลี่ยมจัตุรัสกึ่งกลางใบหน้าโดยไม่ยืด/บีบสัดส่วน
> ก่อนบันทึก ถ้าหาใบหน้าไม่เจอจะ fallback เป็นการครอปกึ่งกลางภาพแบบรักษาสัดส่วน (ไม่ยืด)
> แทนการยืดแบบเดิม พร้อมแจ้งเตือนนักศึกษาทันทีตอนเลือกรูปถ้าไม่พบใบหน้าชัดเจน

## 2.2 ติดตั้งบน Raspberry Pi

คัดลอกโฟลเดอร์ `smartattend_pi/` ไปไว้บน Pi (เช่นผ่าน `scp` หรือ USB) แล้วสั่ง:

```bash
cd smartattend_pi
chmod +x install.sh
./install.sh
```

> ⚠️ ขั้นตอนคอมไพล์ `dlib` **ใช้เวลา 20–40 นาทีบน Pi 4** เป็นเรื่องปกติ
> ปล่อยทิ้งไว้ได้ อย่าปิดเครื่องกลางคัน

จากนั้นตั้งค่าการเชื่อมต่อ:

```bash
cp .env.example .env
nano .env
```

ใส่ `SUPABASE_SERVICE_KEY` ซึ่งหาได้จาก Supabase Dashboard → Project Settings → API

> ต้องใช้ **service_role key** ไม่ใช่ anon key เพราะ Pi ต้องบันทึกข้อมูล
> แทนนักศึกษาโดยไม่ได้ล็อกอินเป็นตัวเขา
> คีย์นี้มีสิทธิ์สูงมาก **ห้ามนำไปใส่ในเว็บหรือแอปมือถือเด็ดขาด**

## 2.3 ทดลองรัน

```bash
source .venv/bin/activate
python pi_agent.py
```

จะเห็นหน้าต่างกล้องพร้อมกรอบสี:

| สีกรอบ | ความหมาย |
|---|---|
| 🔴 แดง | ไม่พบใบหน้า หรือไม่รู้จักคนนี้ |
| 🟡 เหลือง | รู้จักแต่ค่าความเชื่อมั่นยังไม่สูงพอ |
| 🟢 เขียว | ยืนยันตัวตนได้ พร้อมบันทึก |

### ขั้นตอนการทำงาน

1. Pi ถามเซิร์ฟเวอร์ทุก 4 วินาทีว่ามีคาบเรียนเปิดอยู่ไหม
2. เมื่ออาจารย์กด "เปิดคาบเรียน" Pi จะดาวน์โหลดภาพใบหน้าของนักศึกษาในวิชานั้น
   มาสร้างคลังเวกเตอร์อัตโนมัติ
3. นักศึกษาเดินมาหน้ากล้อง → ระบบจดจำ → **ต้องเจอคนเดิมติดกัน 3 เฟรมจึงยืนยัน**
   (กันการทายผิดชั่ววูบ)
4. บันทึกการเข้าเรียนพร้อมภาพหลักฐานและค่าความเชื่อมั่นขึ้นฐานข้อมูล
5. อาจารย์เห็นผลบนหน้าเว็บทันทีแบบเรียลไทม์

> Pi เป็นฝ่ายถามเซิร์ฟเวอร์เอง (polling) เพราะอุปกรณ์อยู่หลัง NAT ของมหาวิทยาลัย
> เซิร์ฟเวอร์จึงยิงคำสั่งเข้ามาหา Pi โดยตรงไม่ได้

## 2.4 ปรับความเข้มงวด

```bash
python pi_agent.py --tolerance 0.40    # เข้มงวดขึ้น (ปฏิเสธง่ายขึ้น)
python pi_agent.py --tolerance 0.50    # ผ่อนปรนลง
python pi_agent.py --camera 1          # ถ้ามีกล้องหลายตัว
python pi_agent.py --no-display        # ไม่เปิดหน้าต่าง (สำหรับรันเบื้องหลัง)
```

ค่าเริ่มต้น 0.45 เลือกให้เข้มงวดกว่ามาตรฐานของ dlib (0.6) เพราะงานเช็คชื่อ
ยอมให้ปฏิเสธคนถูกได้ แต่ยอมให้รับคนผิดไม่ได้

## 2.5 ตั้งให้รันอัตโนมัติเมื่อเปิดเครื่อง

```bash
sudo cp smartattend.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable smartattend
sudo systemctl start smartattend

sudo systemctl status smartattend     # ดูสถานะ
journalctl -u smartattend -f          # ดูบันทึกการทำงานแบบสด
```

---

## ข้อควรรู้ก่อนใช้งานจริง

**ต้องมีนักศึกษาลงทะเบียนใบหน้าอย่างน้อย 3–5 คน**
ถ้ามีคนเดียว ระบบจะไม่มีตัวเปรียบเทียบและอาจทายเป็นคนนั้นเสมอ
โปรแกรมจะเตือนในบันทึกการทำงานถ้าพบว่ามีข้อมูลน้อยเกินไป

**Pi 4 ประมวลผลได้ประมาณ 2–4 เฟรมต่อวินาที** ซึ่งเพียงพอสำหรับการเช็คชื่อ
โค้ดย่อภาพลง 4 เท่าก่อนตรวจจับเพื่อความเร็ว โดยไม่กระทบความแม่นยำมากนัก

**เวลาที่ใช้ตัดสินว่ามาสายอ้างอิงจากเซิร์ฟเวอร์** ไม่ใช่นาฬิกาของ Pi
เพราะนาฬิกา Pi อาจคลาดเคลื่อนหากไม่ได้ซิงก์เวลา

---

## โครงสร้างไฟล์ฝั่ง Python

```
smartattend_pi/
├── face_recognizer.py     อัลกอริทึมจดจำใบหน้า (embedding + วัดระยะห่าง)
├── supabase_client.py     เชื่อมต่อฐานข้อมูล ดึงข้อมูล บันทึกการเข้าเรียน
├── pi_agent.py            โปรแกรมหลัก ควบคุมกล้องและวงจรการทำงาน
├── install.sh             สคริปต์ติดตั้งอัตโนมัติ
├── smartattend.service    ตั้งให้รันอัตโนมัติเมื่อเปิดเครื่อง
└── .vscode/launch.json    กด F5 รันได้ใน VS Code
```
