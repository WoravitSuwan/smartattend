# ── ไลบรารีที่ติดตั้งผ่าน pip ──────────────────────────────
# หมายเหตุ: opencv, picamera2 และ dlib ติดตั้งผ่าน apt ใน install.sh
# เพราะเวอร์ชันจาก apt คอมไพล์มาให้ตรงกับสถาปัตยกรรมของ Pi แล้ว
# venv จึงต้องสร้างด้วย --system-site-packages เพื่อให้มองเห็น

face-recognition==1.3.0
face-recognition-models==0.3.0
numpy<2

# เชื่อมต่อฐานข้อมูล
httpx==0.27.2
python-dotenv==1.0.1
